import { GlobalProvider } from "./context/GlobalContext";

function App() {
  return (
    <GlobalProvider>
      Edit <code>src/App.js</code> and save to reload.
    </GlobalProvider>
  );
}

export default App;
